import { ProductsRegistryContent } from "./products-registry-content";

/** Real M2 registry route. The legacy dashboard mock remains under /tables. */
export default function ProductsPage() {
  return <ProductsRegistryContent />;
}
